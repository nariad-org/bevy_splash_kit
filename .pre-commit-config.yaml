repos:
  - repo: local
    hooks:
      - id: rustfmt
        name: rustfmt
        entry: cargo fmt -- --check
        language: system          # Uses the system’s Cargo installation
        types: [rust]             # Limits the hook to *.rs files
